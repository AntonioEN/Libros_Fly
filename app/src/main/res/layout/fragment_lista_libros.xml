<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:background="@color/lima">

        <EditText
            android:id="@+id/txtBuscarLibro"
            android:layout_width="wrap_content"
            android:layout_height="47dp"
            android:hint="Introduce titulo del libro" />

        <Button
            android:id="@+id/btnBuscar"
            android:layout_width="64dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            app:icon="@android:drawable/ic_search_category_default" />

        <Button
            android:id="@+id/btnSugerencia"
            android:layout_width="116dp"
            android:layout_height="55dp"
            android:layout_marginLeft="15dp"
            android:text="Sugerencia"
            android:textSize="12dp" />

    </LinearLayout>


    <androidx.recyclerview.widget.RecyclerView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="0dp"
        android:layout_marginRight="0dp"
        app:layoutManager="LinearLayoutManager"
        tools:listitem="@layout/fragment_lista_libros"
        android:background="@color/pradera"/>

</LinearLayout>
